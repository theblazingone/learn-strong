#include <iostream>

using namespace std;

int main(){

    int t;
    cin>>t;

    while (t--){

        string s;
        cin>>s;

        int win_p=0, lose_p=0;

        puts ( s[s.length()-1] == '1' ? "WIN" : "LOSE");
    }

    return 0;
}