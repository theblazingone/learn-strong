#include <iostream>
#include <cstring>

using namespace std;

int main(){
    int n;
    char a[10];
    scanf("%d", &n);
    while (n--){
        scanf("%s", a);
        printf("%ld\n", a[0]-'0'+a[strlen(a)-1]-'0');
    }
    return 0;
}